<Window x:Class="Perrrfect_stay.AdminRequestsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Perrrfect_stay"
        mc:Ignorable="d"
        Title="AdminRequestsWindow" Height="450" Width="800">
    <Grid Background="#F8F4F2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="20" Background="#5A4634">
            <TextBlock Text="Pending Requests 📋" FontSize="24" FontWeight="Bold"
      Foreground="White" 
        HorizontalAlignment="Center"/>

        </Border>

        <Button Grid.Row="1" Content="&lt; Back" 
         Click="Back_Click" 
         HorizontalAlignment="Left"
         Background="#E8A87C" Foreground="White" Margin="20,10" Padding="10,5"/>

        <DataGrid Grid.Row="2" x:Name="RequestGrid"
               Margin="20" AutoGenerateColumns="False"
               IsReadOnly="True" Background="White">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="User ID" Binding="{Binding UserId}" Width="80"/>
                <DataGridTextColumn Header="Cat ID" Binding="{Binding CatId}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Data" Binding="{Binding RequestData}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
