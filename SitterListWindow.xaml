<Window x:Class="Perrrfect_stay.SitterListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Perrrfect_stay"
        mc:Ignorable="d"
        Title="SitterListWindow" Height="450" Width="800">
    
        <Grid Background="#F8F4F2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#5A4634" Padding="20">
                <TextBlock Text="Find a Cat Sitter 🐾" FontSize="24" FontWeight="Bold"
                   Foreground="White" HorizontalAlignment="Center"/>
            </Border>

            <Button Grid.Row="1" Content="&lt; Back"
            Click="Back_Click"
            HorizontalAlignment="Left"
            Margin="20,10" Padding="10,5"
            Background="#E8A87C" Foreground="White"/>

            <ScrollViewer Grid.Row="2">
                <ItemsControl x:Name="SitterList" Margin="20">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="220" Height="250" Margin="10"
                            Background="White" CornerRadius="15"
                            BorderBrush="#E8A87C" BorderThickness="1">
                                <StackPanel HorizontalAlignment="Center"
                                    VerticalAlignment="Center" Margin="10">
                                    <TextBlock Text="🐾" FontSize="50"
                                       HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Name}"
                                       FontSize="16" FontWeight="Bold"
                                       Foreground="#5A4634"
                                       HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Location}"
                                       FontSize="12" Foreground="#7A6A58"
                                       HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding PricePerDay}"
                                       FontSize="14" Foreground="#E8A87C"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"/>
                                    <Button Content="View Profile"
                                    Click="ViewProfile_Click"
                                    Margin="0,10,0,0"
                                    Background="#E8A87C"
                                    Foreground="White"
                                    FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
   
</Window>
