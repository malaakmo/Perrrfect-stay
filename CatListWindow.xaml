<Window x:Class="Perrrfect_stay.CatListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Perrrfect_stay"
        mc:Ignorable="d"
        Title="CatListWindow" Height="450" Width="800">
   
        <Grid Background="#F8F4F2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
               
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Our Cate🐾"
            FontSize="28" FontWeight="Bold"
            Foreground="#5A4634" HorizontalAlignment="Center"
            Margin="0,20,0,10"/>

            <TextBox Grid.Row="1" x:Name="SearchBox"
                       Height="38" Margin="20,0,20,15"
                       Background="#FFF3E8" BorderBrush="#E8A87C"
                       Padding="10"    FontSize="14"/>
            <ScrollViewer Grid.Row="2">

            <ItemsControl x:Name="CatList" Margin="20">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="180" Height="220" Margin="10"
                                Background="White" CornerRadius="15"
                                BorderBrush="#E8A87C" BorderThickness="1">
                            <StackPanel HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                <TextBlock Text="🐱" FontSize="50"
                                           HorizontalAlignment="Center"/>

                                <TextBlock Text="{Binding Name}"
                                           FontSize="16" FontWeight="Bold"
                                           Foreground="#5A4634"
                                          HorizontalAlignment="Center"/>

                                <TextBlock Text="{Binding Breed}"
                                           FontSize="12" Foreground="#7A6A58"
                                          HorizontalAlignment="Center"/>

                                <Button Content="Details"
                                        Click="Details_Click"
                                        Margin="10,10,10,0"
                                        Background="#e8a87c"
                                        Foreground="White"
                                        FontWeight="Bold"/>

                            </StackPanel>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
             </ItemsControl>
        </ScrollViewer>






        
        
    </Grid>
</Window>
